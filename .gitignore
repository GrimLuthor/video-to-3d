# --- Python environments / caches ---
venv/
.venv/
env/
__pycache__/
*.pyc
*.pyo

# --- Large inputs: drop your own videos into capture/, not tracked in git ---
capture/*
!capture/.gitkeep

# --- Generated data: regenerated per run; transferred by zipping, not git ---
output/*
!output/.gitkeep

# --- Not part of the Python pipeline ---
VideoTo3D/
VIdeoTo3D/
unity_export/
report_assets/
partner_guide/
report/
viewer_build/

# --- LaTeX build artifacts (in case any .tex is ever tracked) ---
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fdb_latexmk
*.fls

# --- OS cruft ---
Thumbs.db
.DS_Store

# --- Part B report images + the reference paper (not pipeline code) ---
report_assets_partB/
An Approach to Robust ICP Initialization.pdf

# --- stray Open3D viewer setting dumps ---
RenderOption_*.json

# --- machine-specific overnight batch helper (absolute paths) ---
overnight_batch.ps1
